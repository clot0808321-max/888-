[phases.setup]
nixPkgs = ["nodejs_20", "python3", "gcc", "gnumake", "pkg-config"]

[phases.install]
cmds = ["rm -rf node_modules package-lock.json", "npm install --omit=dev --no-audit --no-fund"]

[start]
cmd = "node server.js"
